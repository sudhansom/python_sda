# create a tuple
my_tuple = ("boil water", "insert egg", "wait 5min", "eat")
# finding the length
print(len(my_tuple))
print(my_tuple[2])
# slicing the tuple's last two elements
print(my_tuple[-2:])
# checking the index of a certain element in a tuple
print(f"the position of 'boil water is : {my_tuple.index('boil water')}")
